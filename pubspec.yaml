name: counter_animation
description: A very simple (50 lines of code, see Git rep) flutter package for animating a counter that decrements or increments an integer.
version: 0.0.1
author: Thor Garske Andresen<thorg3@gmail.com>
homepage: https://github.com/thorandresen/counter_animation

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
